What they will submit:

approx.c
  - thier implementation
compile_command.txt
  - contains a text where the first line is a compilation command that uses clang to compile with -Wall -o approx approx.c
